<h1>Breweries</h1>

<ul class="cards">
    <% @breweries.each do |b| %>
        <li class="card"><%= link_to b.name, brewery_path(b) %></li>
    <% end %>
</ul>

<%= button_to "New Brewery", new_brewery_path, method: "get" %>